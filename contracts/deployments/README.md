# Deployments

Deployment JSON files are generated here by `script/Deploy.s.sol`.

Local and test deployment files are ignored by default. For a public production
release, add the reviewed deployment JSON intentionally and include the matching
commit hash, chain id, explorer links, constructor arguments, and multisig owner
addresses in the release notes.
