# Deployments

Deployment JSON files are generated here by `script/Deploy.s.sol`.

Generated non-production deployment files are ignored by default. The reviewed
Arbitrum One record, `3sat-42161.json`, is the only deployment JSON admitted by
the production release manifest. Include its matching commit hash, explorer
links, constructor arguments, and multisig owner addresses in the release notes.
